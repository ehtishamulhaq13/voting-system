name: voting_app
description: "Voting System App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.6.0
  shared_preferences: ^2.3.3
  image_picker: ^1.0.7
  google_fonts: ^6.1.0
  animate_do: ^3.3.4
  flutter_animate: ^4.5.0
  percent_indicator: ^4.2.3
  lottie: ^3.1.0
  glassmorphism: ^3.0.0
  flutter_staggered_animations: ^1.1.1
  shimmer: ^3.0.0
  fl_chart: ^0.68.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/lottie/
